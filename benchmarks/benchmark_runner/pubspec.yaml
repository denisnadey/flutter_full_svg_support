name: full_svg_flutter_benchmark_runner
description: Pure Dart parser/loader microbenchmarks
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  full_svg_flutter: ^1.0.1
  flutter_svg: ^2.0.14
  benchmark_harness: ^2.3.1

dev_dependencies:
  lints: ^4.0.0
